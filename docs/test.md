# Untitled

Write your notes here $y = x+b$.

## yes
$$\int_0^\infty e^{-x}\,dx = 1$$

## A streamed agent edit

The agent just appended this section to `test.md`. KaTeX should typeset:

- inline math: $e^{i\pi} + 1 = 0$
- a display block:

$$
\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}
$$

And a tiny code block:

```python
def double(x):
    return x * 2
```
